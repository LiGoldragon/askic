//! askic — aski compiler (v0.16)
//!
//! The sema engine compiler stage.
//! Takes askicc's data-tree, parses .aski programs,
//! produces typed parse tree for semac.

fn main() {
    eprintln!("askic v0.16 — not yet implemented");
    eprintln!("See: ~/git/aski/spec/engine.md");
    std::process::exit(1);
}
